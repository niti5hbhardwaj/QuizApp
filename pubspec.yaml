name: quiz_app
description: A quiz app using ChatGPT
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  http: ^1.0.0
  cupertino_icons: ^1.0.2
  flutter_spinkit: ^5.2.0
  lottie: ^2.4.0
  animations: ^2.0.7
  flutter_svg: ^2.0.7
  firebase_core: ^2.14.0
  firebase_auth: ^4.6.3
  cloud_firestore: ^4.8.2
  firebase_storage: ^11.2.4
  image_picker: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/animations/
    - assets/images/
    - assets/subject_icons/